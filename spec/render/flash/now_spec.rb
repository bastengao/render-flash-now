require 'spec_helper'

describe Render::Flash::Now do
  it 'has a version number' do
    expect(Render::Flash::Now::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
